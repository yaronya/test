version: 1
deploy:
    steps:
        setupVariables:
          after:
            - env
            - aws --version
            - gcloud --version
            - az --version
            - sudo apk add docker
            - docker --help
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
