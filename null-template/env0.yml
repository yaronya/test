version: 2
deploy:
    steps:
        terraformPlan:
          after:
            - name: "I'm testing how this runs"
              run: env
            - name: "Multiline commands"
              run: |
                echo test
                echo foo
                echo bar
            - name: "Testing env vars"
              run: env
              env:
                yaron1: $sadasd
                yaron2: yaron2
                yaron3: $nope
            - name: "Testing no env vars next step"
              run: env

destroy:
    onCompletion:
      - echo "destroy-onCompletion"
