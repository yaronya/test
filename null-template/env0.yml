version: 1
deploy:
    steps:
        setupVariables:
          after:
            - env
            - aws --version
            - gcloud --version
            - echo $PATH
            - aws sts get-caller-identity
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
