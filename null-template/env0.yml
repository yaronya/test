version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - gcloud --version
            - az --version
            - sudo ps -ef
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
