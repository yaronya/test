version: 1
deploy:
    steps:
        setupVariables:
          after:
            - env
            - aws --version
            - gcloud --version
            - echo $PATH
            - which awsv2
            - awsv2 --version
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
