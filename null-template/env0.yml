version: 2
deploy:
    steps:
        terraformPlan:
          after:
            - name: "I'm testing how this runs"
              run: env
            - name: "Another test that should run in the middle"
              run: exit 1
            - echo no-name

destroy:
    onCompletion:
      - echo "destroy-onCompletion"
