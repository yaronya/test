version: 1
deploy:
    steps:
        setupVariables:
          after:
            - aws --version
            - gcloud --version
            - az --version
            - sudo apk add docker
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
