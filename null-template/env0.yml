version: 1
deploy:
    steps:
        setupVariables:
          after:
            - env
            - kubectl version
            - which kubectl
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
