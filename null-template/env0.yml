version: 1
deploy:
    steps:
        setupVariables:
          after:
            - env
            - kubectl version --client
            - which kubectl
destroy:
    onCompletion:
      - echo "destroy-onCompletion"
