version: 2
deploy:
  steps:
    terraformPlan:
      before:
        - name: Checkov
          use: https://github.com/env0/checkov-plugin
          input:
            directory: .
            flags: --framework terraform

destroy:
    onCompletion:
      - echo "destroy-onCompletion"
